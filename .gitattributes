# The game databases are tracked binary content. Marking them explicitly keeps
# git from attempting text normalisation or a useless diff on 1.5 MB of SQLite.
*.db binary

# Shell scripts must stay LF so they run on the Linux VM regardless of the
# committing host's platform.
*.sh text eol=lf
